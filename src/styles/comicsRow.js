import { StyleSheet } from 'react-native';
import CONFIG from './_CONFIG';

export default comicsRowStyle = StyleSheet.create({
  rowContent: {
    flex: 1,
    justifyContent: 'center',
    paddingTop: 14,
    paddingRight: 20,
    paddingBottom: 14,
    paddingLeft: 22,
    backgroundColor: '#fff',
  },
});
